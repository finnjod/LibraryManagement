# LibraryManagement
Python | Flask | HTML | JavaScript | SQL Allows users to search real-time across 5,200+ books. Filtering by title, author, genre and availability — deployed at an active library to replace slow manual systems. Lightweight fetch requests hit a Flask REST API on each keystroke, querying an optimized SQL database and rendering results with no reloads.
